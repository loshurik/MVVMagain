<UserControl x:Class="MVVMagain.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             >
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.ColumnSpan="2" Grid.RowSpan="2" >
            <DockPanel>
                <TextBlock Text="Тема " HorizontalAlignment="Right"/>
                <TextBlock Text="{Binding CurrentCategory}" HorizontalAlignment="Right" />
            </DockPanel>
            <DockPanel>
                <TextBlock Text="Вопрос за "   HorizontalAlignment="Right"/>
                <TextBlock Text="{Binding CurrentQuestion}" HorizontalAlignment="Right"/>
            </DockPanel>
        </StackPanel>
        
    </Grid>
</UserControl>
